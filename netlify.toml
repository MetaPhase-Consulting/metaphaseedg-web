# Netlify configuration for MetaPhase EDG
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Contact form notifications are configured in the Netlify UI (Forms > Settings)
# to send to info@metaphaseedg.com, which Cloudflare Email Routing forwards to Sophia.
